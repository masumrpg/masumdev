---
sidebar_position: 2
---

# Installation

## Prerequisites

:::danger Peer Dependencies
Make sure you have these peer dependencies installed in your React Native or Expo project. Using incompatible versions may cause unexpected behavior or crashes:

```json
{
  "react": "^18.3.1",
  "react-native": "^0.76.7",
  "react-native-reanimated": "^3.16.7",
  "react-native-gesture-handler": "^2.20.2",
  "react-native-safe-area-context": "^4.12.0"
}
```

### Installing prerequisites package

:::tip Recommended Installation
Use the following command to install all required peer dependencies at once:

- Using npm
```bash
npm install react-native-reanimated react-native-gesture-handler react-native-safe-area-context
```
- Using yarn
```bash
yarn add react-native-reanimated react-native-gesture-handler react-native-safe-area-context
```
- Using bun
```bash
bun add react-native-reanimated react-native-gesture-handler react-native-safe-area-context
```
- Using pnpm
```bash
pnpm add react-native-reanimated react-native-gesture-handler react-native-safe-area-context
```
:::

## Expo Setup

:::tip Expo Installation
If you're using Expo, we recommend using `expo install` instead of `npm install` to ensure compatibility with your Expo SDK version.
:::

### Managed Workflow

1. Install the required dependencies:

```bash
npx expo install react-native-reanimated react-native-gesture-handler react-native-safe-area-context
```

2. Update your `babel.config.js`:

```js
module.exports = function (api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
    plugins: ['react-native-reanimated/plugin'],
  };
};
```

## Installing Library

:::tip Library Installation
Install the `@masumdev/rn-toast` package using your preferred package manager.
:::

#### Using npm

```bash
npm install @masumdev/rn-toast
```

#### Using yarn

```bash
yarn add @masumdev/rn-toast
```

#### Using bun

```bash
bun add @masumdev/rn-toast
```

#### Using pnpm

```bash
pnpm add @masumdev/rn-toast
```

## Troubleshooting

:::danger Common Issues
- For Reanimated issues, ensure you've rebuilt the app after installing the package
- Make sure all peer dependencies versions match the requirements
- If you encounter "Reanimated plugin not found" error, verify your babel.config.js is properly configured
- Metro bundler cache issues can be resolved by clearing the cache: `npm start -c`
- If animations are not working, check that Reanimated is properly configured in your babel.config.js
- For gesture-related issues, ensure react-native-gesture-handler is properly installed and initialized
:::

:::tip Video Tutorial
<div style={{ display: 'flex', flexDirection: 'column', marginTop: '20px', marginBottom: '20px' }}>
  <a href="/docs/rn-toast/tutorial#video-tutorial" style={{ textDecoration: 'none' }}>
    <div style={{ textAlign: 'center', padding: '20px', border: '1px solid #e0e0e0', borderRadius: '8px', width: '100%', background: 'rgb(187, 196, 185)' }}>
      <img src="https://img.youtube.com/vi/ytdpXMd1fqU/maxresdefault.jpg" alt="Tutorial Thumbnail" style={{ width: '100%', borderRadius: '4px', marginBottom: '15px' }} />
      <h3 style={{ margin: '0', color: 'rgb(38, 121, 67)' }}>Watch Implementation Guide</h3>
      <p style={{ margin: '10px 0 0', color: 'rgb(51, 54, 51)' }}>Follow our step-by-step guide to integrate and customize the toast component in your React Native application</p>
    </div>
  </a>
</div>
:::